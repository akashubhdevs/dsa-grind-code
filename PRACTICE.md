# Practice this Before the Next Stream:

> related to maps
### 1. The Frequency Counter
Given a vector of strings, count how many times each string appears. Output the result in alphabetical order (C++ std::map does this automatically because it sorts by key!).

```
Example Input: ["apple", "banana", "apple", "cherry", "banana", "apple"]

Expected Output:

apple: 3

banana: 2

cherry: 1

Hint: Iterate through the vector and use the map like this: myMap[word]++.
```

> related to sets
### 2. The Unique Element Filter
Given a vector of integers that contains duplicates, write a program that removes all duplicate values and prints the remaining unique elements in ascending order.

```
Example Input: [5, 1, 2, 5, 3, 1, 9, 2]

Expected Output: 1 2 3 5 9

```